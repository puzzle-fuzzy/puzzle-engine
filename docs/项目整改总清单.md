# 项目整改总清单

更新时间：2026-06-12

本文只记录当前仍存在的问题。已经验收完成的问题不再展开；每完成一项，需要同步更新本文，并为该项提交对应 git commit。

## 当前验收状态

本轮复核以当前代码为准，执行时间为 2026-06-12。

已通过：

- `bun run typecheck`：通过。
- `bun run lint`：通过。
- `bun run build`：通过。client 主入口 chunk 约 273 kB，CanvasEditor 已独立 chunk。
- `bun run test`：通过，server 236 + worker 24 + billing 31 + provider 108 + shared 41 = 440 tests pass。
- `bun run test:client`：通过，6 个文件、41 个测试通过。
- `bun run test:db`：通过，DB 包 67 个测试通过。
- `rg -n "\bas any\b|\bany\b|@ts-ignore|@ts-expect-error" apps packages`：通过。测试代码不再出现 `as any`（仅 makeValidatedParams helper 内部集中管控 branded type 构造）。

当前判断：

- P0 已全部修复。工程门禁闭环。
- P1-1（Record 收敛）95% 完成，P1-5（响应 DTO）共享类型已建立，渐进迁移。

## P0：必须先修复

### 1. ✅ `bun run lint` 已修复

原问题：

```text
apps/server/src/modules/canvas/videos.ts
  'BillingParams' is defined but never used

apps/server/src/modules/generation/output-parser.ts
  import order error

apps/server/test/canvas-videos.test.ts
  import order error

apps/worker/src/task-processor.ts
  'BillingParams' is defined but never used
  named import order error

packages/billing/test/calculate.test.ts
  'BillingParams' is defined but never used

packages/provider/src/dashscope-client.ts
  import order error

packages/provider/src/index.ts
  named export order error

packages/shared/src/index.ts
  export order error
```

处理结果：

- 已删除未使用 import，并按 ESLint 规则修复 import/export 顺序。
- 已验证：

```bash
bun run lint
```

提交要求：此项单独提交 commit，例如 `chore: fix lint import ordering`。

### 2. ✅ `bun run test` 已修复

原失败文件：

- `apps/server/test/generate-routes.test.ts`

失败用例：

- `generate routes > POST /api/generate > referenceFileIds 全部属于当前用户时正常创建记录`

现象：

测试期望 provider 收到：

```ts
{ prompt: '你好' }
```

实际收到：

```ts
{ prompt: '你好', __validatedBrand: true }
```

判断：

- 这是 `ValidatedModelParameters` branded type 引入后的测试断言未同步。
- 不应为了测试通过把业务代码改回弱类型。
- 应修测试 helper/断言，让测试验证业务关键信息：prompt 保留、referenceUrls 正确传递、参数已通过 validated params 路径。

处理结果：

- 已将断言改为验证关键业务字段，而不是要求 branded params 与原始对象完全相等。
- `generate-routes.test.ts` 已使用 `makeValidatedParams()`，不在该文件里用 `as any` 构造 branded params。
- 已验证：

```bash
bun test --cwd apps/server test/generate-routes.test.ts
bun run test
```

提交要求：此项单独提交 commit，例如 `test: align generate route assertions with validated params`。

### 3. ✅ 测试代码 `as any` 已清理

原问题：

- 9 个 server 测试文件中 mock `validateAndMerge` 使用 `params as any` 构造 branded type。

处理结果：

- 新增 `makeValidatedParams(params)` helper（`apps/server/test/helpers/test-factory.ts`）
- helper 内部集中管控 branded type 构造，替代各文件散落的 `as any`
- `rg any` 在测试代码中不再命中

## P1：上线前应修复

### 4. `Record<string, unknown>` 已收敛至合法边界 — 残余为已知 Category A

已完成：

- [x] `GenerationInputParams` 域类型 — DB `generation_records.inputParams` JSONB 列从 `Record<string, unknown>` 改为 `GenerationInputParams`（信封字段 source/projectId/shotId/referenceFileIds 显式声明）
- [x] `ValidatedModelParameters` branded type — `validateAndMerge()` 为唯一构造路径，`DashScopeClient` 公开方法只接受此类型
- [x] Provider output types — 移除 `extends Record<string, unknown>`，`Text/Image/VideoTaskProviderOutput` 变为独立接口
- [x] `TaskStatus.output` — 从 `Record<string, unknown>` 改为 `DashScopeTaskOutput`（含 video_url, results, video_duration, duration）
- [x] `DashScopeTaskResultItem` — 替代 `Array<Record<string, unknown>>`
- [x] `BillingParams` + `extractBillingParams` — billing calculateCost/estimateCost 从 `Record<string, unknown>` 改为 `BillingParams`
- [x] `GenerationContext.parameters/inputParams` — 从 `Record<string, unknown>` 改为 `ValidatedModelParameters/GenerationInputParams`
- [x] Worker `task-processor` — `inputParams` 改为 `GenerationInputParams | null`，`output` 改为 `DashScopeTaskOutput`
- [x] `CanvasVideoParameters` — 移除 `extends Record<string, unknown>`，变为独立接口
- [x] Canvas 模块（analysis/characters/locations/references/storyboard）— 全部改用 `validateAndMerge`
- [x] output-parser — 接受 typed provider output union，不再接受 `Record<string, unknown>`
- [x] `GenerationNotifyPayload` — 从 shared 移入 db/domain-types.ts，`notify.ts` 接受类型化 payload
- [x] Category A 位置全部 JSDoc 标注（audit detail, file metadata, credit metadata, workflow input/output, pipeline snapshots, SSEPipelineNodeEvent.data, notifyNode data, sse.ts isObject/str, CanvasLayoutNode/Edge.data）

保留为合法 `Record<string, unknown>` 的位置（Category A，均有 JSDoc 说明）：

- `applyMappings/buildRequestBody` 内部累积器（DashScope API 请求构建边界）
- `sse.ts isObject/str`（SSE JSON 解析边界辅助）
- `audit_logs.detail`（审计详情各异）
- `uploaded_files.metadata`（文件元信息各异）
- `credit_transactions.metadata`（交易元信息各异）
- `workflows.input/output`（预研占位）
- `canvas_pipeline_runs` snapshots（管道审计快照）
- `CanvasLayoutNode/Edge.data`（前端 React Flow 状态）
- `DashScopeTaskOutput[key: string]: unknown`（DashScope 额外字段）
- `GenerationInputParams[key: string]: unknown`（DB JSONB 存储边界）
- `GenerateRequest.parameters`（API 未验证入口）
- `WorkspaceState.parameters`（UI 表单状态）
- `notifyNode data`（SSE 推送数据不透明）
- `SSEPipelineNodeEvent.data`（SSE 管道节点数据不透明）

残余宽泛 record（仍需关注但非紧急）：

- `apps/client/src/api/client.ts` 的 Eden 错误提取（外部 API 错误边界）
- `apps/client/src/stores/workspace.ts` 的 workspace parameters（UI 表单状态，服务端校验后才类型化）
- `packages/db/src/repositories/workflows.repo.ts` 的 workflow input/output（预研占位，待真正启用时按 workflow type 定义 schema）

完成程度：95%。核心业务层 DTO 收敛已完成，仅 workflow 预研和 client UI 表单状态保留宽泛 record，均有明确标注。

### 5. 路由响应 DTO 语义 — 共享类型已建立，渐进迁移

已完成：

- [x] 共享 DTO 类型定义：`MutationOkResponse`, `EntityResponse<T>`, `ListResponse<T>`, `RecordResponse<T>`, `ApiErrorResponse`（packages/shared/src/api-response.ts）
- [x] Canvas pipeline phase + 视频重试入口已使用 `AcceptedResponse`

现状：

- 其他 API 仍返回 `{ success: true }` / `{ success: false }`。
- 简单同步 mutation 返回 `success` 不一定是 bug，但需要显式 DTO 类型命名。
- 新增路由应使用共享 DTO，现有路由可渐进迁移。

完成程度：40%。共享类型已建立，渐进迁移中。

### 6. ✅ SSE 认证口径已统一

原问题：

- 前端 `sseClient.connect()` 已支持刷新后通过 httpOnly cookie 建立连接，并使用 `credentials: 'include'`。
- 服务端 `createAuthPlugin()` 也支持 cookie、API Key Bearer、JWT Bearer。
- 但 `apps/server/src/routes/sse.ts` 注释和 OpenAPI description 仍写“仅支持 Bearer header 认证”。

风险：

- 后续维护者可能误以为刷新后必须恢复内存 token，重新引入 localStorage 或 query token。

处理结果：

- 已更新 SSE 路由注释和 OpenAPI description：
  - 浏览器端优先 httpOnly cookie + `credentials: include`。
  - 编程式客户端可使用 `Authorization: Bearer <jwt>`。
  - query token 不支持。
- 已补服务端认证测试：
  - cookie JWT 可通过 SSE 等价 require-auth 路径。
  - Bearer JWT 可通过。
  - 无认证、无效 Bearer、query token 均返回 401。

已验证：

```bash
bun test --cwd apps/server test/auth-plugin-sse-token.test.ts
```

### 7. ✅ dedupeKey 已改为稳定 SHA-256

原位置：

- `apps/server/src/routes/generate.ts`
- `apps/server/src/routes/openai-gateway.ts`

原问题：

```ts
const dedupeKey = `${userId}:${model}:${JSON.stringify(parameters)}`
```

- key 顺序敏感。
- prompt 或参数较长时 key 过长。
- OpenAI gateway 也用了同类策略。
- 当前 schema 中 `dedupeKey` 已是 `text`，但语义仍不够稳定。

处理结果：

- 新增 `apps/server/src/utils/dedupe-key.ts`：
  - canonical stringify：递归排序 object key。
  - SHA-256 输出：`sha256:<hex>`。
  - 普通 generate 和 OpenAI gateway 共用同一个 helper。
- 普通 generate 使用已校验参数 `validatedParams` 生成去重键。
- 普通 generate 将 `referenceFileIds` 纳入去重输入，避免同 prompt 但不同参考文件被误判为重复任务。
- OpenAI gateway 不再手写 `JSON.stringify(parameters)` 拼接。
- 已补测试：
  - 不同 key 顺序的等价参数生成相同 dedupeKey。
  - referenceFileIds 变化会生成不同 dedupeKey。
  - generate route 层验证不同参数顺序仍传入相同 dedupeKey。

已验证：

```bash
bun test --cwd apps/server test/dedupe-key.test.ts
bun test --cwd apps/server test/generate-routes.test.ts
bun test --cwd apps/server test/openai-gateway.test.ts
bun run lint
bun run typecheck
```

## P2：质量与产品化改进

### 8. fire-and-forget 测试仍会产生后台错误日志

现象：

- `bun run test` 中多个 Canvas route 测试虽然通过，但后台任务在 route 返回后报“项目不存在”“镜头不存在”“没有失败的镜头可以重试”等错误日志。

判断：

- 当前测试关注“立即返回 accepted”，但没有很好地隔离 fire-and-forget 后台任务。
- 日志不是失败，但会污染测试输出，掩盖真实错误。

解决办法：

- route 测试中 mock phase service，使后台任务可控完成。
- 或为 fire-and-forget helper 注入 test scheduler，在测试中同步 flush。
- 保留至少一组测试覆盖后台失败会写 pipeline run failed。

### 9. 产品化能力仍是骨架级实现

已存在：

- API Key 基础认证。
- credit 表和 repository 骨架。
- audit log 服务。
- notifications route。
- OpenAI gateway 基础 route。
- requestId、waitForDb、rate-limit 基础设施。

仍需确认：

- credit reserve/debit/refund 是否真正接入生成主流程。
- notification 是否有真实触发器。
- audit 是否覆盖所有关键动作。
- OpenAI gateway 是否应该作为近期产品能力保留，还是先隐藏入口。
- metrics/health 是否满足部署可观测性。

这些不是当前 P0，但上线前需要产品决策和验收。

## 建议执行顺序

1. 继续收敛残余宽泛 `Record<string, unknown>`（P1-4 残余）。
2. 统一响应 DTO 语义（P1-5）。
3. 清理 fire-and-forget 测试噪声（P2-8）。
4. 产品化骨架能力决策与验收（P2-9）。

## 验收命令

每轮整改后至少运行：

```bash
bun run typecheck
bun run lint
bun run build
bun run test
bun run test:client
bun run test:db
rg -n "\bas any\b|\bany\b|@ts-ignore|@ts-expect-error" apps packages
```

完成定义：

- 上述命令全部通过。
- `rg any` 只允许出现在注释说明、tsconfig 模板注释或第三方声明不可控场景，测试代码不例外。
- 跨模块边界必须有明确 DTO/parser 和能失败的测试。
- 每个独立待办完成后，必须提交对应 git commit，不混入其他待办。
